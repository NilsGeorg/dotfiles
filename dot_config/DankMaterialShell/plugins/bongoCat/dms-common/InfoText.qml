import QtQuick
import qs.Common
import qs.Widgets

StyledText {
    id: root
    width: parent.width
    text: ""
    font.pixelSize: Theme.fontSizeSmall
    color: Theme.surfaceVariantText
    wrapMode: Text.WordWrap
}
